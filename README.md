# Reel-Counter
ReelCounter is a Java Swing desktop application that tracks daily reel swipes using SQLite for local data storage. It automatically saves and restores today's swipe count, maintains complete daily history, fills skipped dates with zero entries, and provides an easy-to-use interface for viewing swipe records and progress over time.
